export type ModelUserProgress = {
  user_id: string;
  coins: number;
  level: number;
  current_xp: number;
  remaining_xp: number;
  total_duration: number;
  last_rewarded_duration: number;
  updated_at: string;
};
